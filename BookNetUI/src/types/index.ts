export interface UserProfile {
  username?: string;
  email?: string;
  firstName?: string;
  lastName?: string;
  token?: string;
}
export interface IBook {
  id: number
  title: string
  author: string
  imageUrl: string
}